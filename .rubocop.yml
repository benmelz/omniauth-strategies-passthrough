require:
  - rubocop-performance
  - rubocop-rake
  - rubocop-rspec

AllCops:
  Exclude:
    - bin/rake
    - gemfiles/**/*
  NewCops: enable
  TargetRubyVersion: 3.2

RSpec/SpecFilePathFormat:
  CustomTransform:
    OmniAuth: omniauth
